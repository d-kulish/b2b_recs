# Django Database Configuration
# Copy this file to .env and fill in actual values
#
# Usage: cp .env.example .env

# Database Engine (postgresql or sqlite)
DB_ENGINE=postgresql

# PostgreSQL Connection (via Cloud SQL Proxy)
DB_NAME=b2b_recs_dev
DB_USER=django_user
DB_PASSWORD=<ask-team-for-password>
DB_HOST=127.0.0.1
DB_PORT=5433

# Cloud SQL Instance Details
# Instance: memo2-db (in project memo2-456215)
# Connection: memo2-456215:europe-central2:memo2-db
# The cloud-sql-proxy must be running (use ./start_dev.sh)

# Note: To use SQLite instead, set DB_ENGINE=sqlite or delete this file
