"""
URL routes for pipelines.

Note: Quick Test endpoints have been moved to ml_platform/experiments/urls.py
which uses TFX pipelines instead of KFP.
"""
from django.urls import path

urlpatterns = [
    # Quick Test routes moved to experiments app (TFX-based)
]
